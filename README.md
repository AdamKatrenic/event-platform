# Spring Boot 3 + Apache Kafka Demo

A simple demonstration of integrating **Spring Boot 3 (Java 21)** with **Apache Kafka** running inside a Docker container. This project showcases asynchronous message producing and consuming using complex objects in **JSON** format.

---

## 🛠️ Application Architecture

The application is built on the principle of loose coupling using the Publish-Subscribe pattern:
